import { useNavigate } from "react-router-dom";
import { Scissors } from "lucide-react";

const Index = () => {
  const navigate = useNavigate();
  return null; // Auth page handles the root route
};

export default Index;
